Create an class "Observable" with three methods (add, delete, notify) and a vector of observers
A class Observer with a virtual pure method notify and inherit them with the real observable and observer
Put rules in the class achievement
